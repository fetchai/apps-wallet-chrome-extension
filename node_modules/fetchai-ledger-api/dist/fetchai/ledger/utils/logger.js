"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.setLogger = exports.logger = void 0;

var _winston = require("winston");

// creates a logger object with format:
// <timestamp> <level>: <message>
let logger = (0, _winston.createLogger)({
  level: 'debug',
  format: _winston.format.combine(_winston.format.colorize(), _winston.format.timestamp({
    format: 'YYYY-MM-DD HH:mm:ss'
  }), _winston.format.printf(info => `${info.timestamp} ${info.level}: ${info.message}`)),
  transports: [new _winston.transports.Console()]
}); // default: silent all logging levels

exports.logger = logger;
logger.transports.forEach(t => t.silent = false);
exports.logger = logger = {};

logger.info = () => {}; // allows other application to set its logger


const setLogger = _logger => {
  exports.logger = logger = _logger;
};

exports.setLogger = setLogger;